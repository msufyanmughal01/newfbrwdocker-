import { SpreadsheetClient } from '@/components/spreadsheet/SpreadsheetClient';

export const metadata = { title: 'Spreadsheet — Easy Digital Invoice' };

export default function SpreadsheetPage() {
  return <SpreadsheetClient />;
}
